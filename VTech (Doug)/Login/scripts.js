function login() {
    // Aqui você pode adicionar a lógica de validação e envio dos dados de login
    const email = document.getElementById('email').value;
    const senha = document.getElementById('senha').value;
    
    alert(`Email: ${email}\nSenha: ${senha}`);
}
